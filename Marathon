#include <bits/stdc++.h>
using namespace std;

void solve() {
    int n;
    cin>>n;
    int counter=0;
    while (n--) {
        int a,b,c,d;
        cin>>a>>b>>c>>d;
        counter=0;
       if (b>a) {
           counter++;
       }
        if (c>a) {
            counter++;
        }
        if (d>a) {
            counter++;
        }
        cout<<counter<<endl;

    }

}
int main() {

    solve();

}
