#include <bits/stdc++.h>
using namespace std;

void Solve() {
    int n;
    cin>>n;
    while (n--) {
        int a,b;
        cin>>a>>b;
        cout<<min(a,b)<<" "<<max(a,b)<<endl;
    }
}

int main() {
    Solve();
}
