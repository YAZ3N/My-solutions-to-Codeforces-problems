#include <bits/stdc++.h>
using namespace std;

void Solve() {

    int n;
    cin>>n;
   int arr[n];
    int freq[101]={};
    for (int i=0;i<n;i++) {
        cin>>arr[i];
        freq[arr[i]]++;
    }
 cout<<*max_element(freq, freq + 101);

}
int main() {
    Solve();
}
