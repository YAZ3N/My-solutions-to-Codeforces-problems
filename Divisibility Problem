#include <bits/stdc++.h>
using namespace std;

void Solve() {
    int n;
    cin>>n;

    while (n--) {
        int a,b;
        cin>>a>>b;
        if (a % b == 0) {
            cout << 0 << "\n";
        } else {
            cout << b - (a % b) << "\n";
        }
    }
}
int main() {
    Solve();
}
