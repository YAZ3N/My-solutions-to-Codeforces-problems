#include <bits/stdc++.h>
using namespace std;
 
void Solve(int x) {
 
    if (x==0)
        return;
 
     cout<<x;
    if (x>1) {
        cout<<" ";
    }
    Solve(x-1);
 
}
int main() {
    int x;
    cin>>x;
    Solve(x);
} 
