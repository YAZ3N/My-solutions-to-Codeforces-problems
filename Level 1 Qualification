#include <bits/stdc++.h>
using namespace std;
 
void Solve() {
 
    int n,k;
    cin>>n>>k;
 
  if (n>=k) {
      cout<<"Qualified";
  }
    else {
        cout<<"Disqualified";
    }
}
int main() {
 
    Solve();
 
}
