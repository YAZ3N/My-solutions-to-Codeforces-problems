#include <bits/stdc++.h>
#define ll long long
using namespace std;

void Solve() {
 int a,b;
    cin>>a>>b;
    string s;
    cin>>s;
    for (int i=0;i<s.size();i++) {
        if (s[a]!='-') {
            cout<<"No";
            return;
        }
    }
    for (int i=0;i<s.size();i++) {
        if (i==a) {
            continue;
        }
        else if (s[i]<'0'|| s[i]>'9') {
            cout<<"No";
            return;
        }
    }
    cout<<"Yes";
}

int main() {
    Solve();
}
