#include <iostream>
#include <iomanip>
using namespace std;
void Solve() {
	int n;
	cin >> n;
	double* arr = new double[n];
	double sum = 0;
	for (int i = 0;i < n;i++) {
		cin >> arr[i];
		sum += arr[i];
	}
	double avg = sum / n;
	cout << fixed << setprecision(6)<<avg;
}
int main() {
	Solve();
}
