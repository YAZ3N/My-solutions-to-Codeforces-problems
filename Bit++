#include <iostream>
 
using namespace std;
 
int main(){
 
   int n;
   cin>>n;
   string s;
   int X=0;
 
   while(n--){
    cin>>s;
    if(s[0]=='+' || s[2]=='+'){
        X++;
    }
    else if(s[0]=='-' || s[2]=='-'){
        X--;
    }
   }
   cout<<X;
}
